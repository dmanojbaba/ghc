{
  "name": "redirect",
  "version": "0.0.0",
  "devDependencies": {
    "vitest": "^4.1.5",
    "wrangler": "^4.14.1"
  },
  "private": true,
  "scripts": {
    "start": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest"
  }
}
